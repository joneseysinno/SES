[package]
name = "ses-shell"
description = "SES layout shell — workspaces, page split trees, and data-flow bus"
publish = false
edition.workspace = true
version.workspace = true
license.workspace = true

[lints]
workspace = true

[features]
default = []
dev-workspaces = []

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
